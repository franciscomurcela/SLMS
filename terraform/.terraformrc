# Terraform configuration for better stability
# Place this file in your home directory (~/.terraformrc)
# Or set environment variable: export TF_CLI_CONFIG_FILE=terraform/.terraformrc

plugin_cache_dir = "$HOME/.terraform.d/plugin-cache"
