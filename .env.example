# ======================================
# Variáveis de Ambiente - Desenvolvimento Local
# ======================================
# Copie este ficheiro para .env e ajuste conforme necessário
# IMPORTANTE: Adicione .env ao .gitignore!

# PostgreSQL Database (container local)
POSTGRES_DB=slms_database
POSTGRES_USER=slmsadmin
POSTGRES_PASSWORD=localpassword123

# Backend Spring Boot
DB_URL=jdbc:postgresql://db:5432/slms_database
DB_USERNAME=slmsadmin
DB_PASSWORD=localpassword123
SPRING_PROFILES_ACTIVE=dev

# Frontend
API_URL=backend:8080

# Keycloak (se usado)
KEYCLOAK_URL=http://localhost:8081
KEYCLOAK_REALM=ESg204
KEYCLOAK_CLIENT_ID=slms-client

# Supabase (se usado localmente)
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=your-anon-key-here
